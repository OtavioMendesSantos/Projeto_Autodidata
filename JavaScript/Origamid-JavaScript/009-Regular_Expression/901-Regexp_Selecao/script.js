// Procura: a
const padraoRegexp = /a/;

const texto = 'JavaScript';
const novoTexto = texto.replace(padraoRegexp, 'B');
// JBvaScript

console.log(novoTexto);

/* Site para testar Regexp: https://regexr.com/7usvt */