const {gets, print} = require('./funcoes_auxiliares') // código que o vs code sugeriu -> import funcoes from './funcoes_auxiliares';

print(gets());

/* 
const pessoa = {
    nome: "Victor"
};

const { nome } = pessoa // const nome = pessoa.nome 
*/